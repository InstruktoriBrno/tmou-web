{block content}
    <div id="banner">
        <h1 n:block=title>Úprava stránky {$page->getTitle()} {if $page->getEvent() !== null}v {$page->getEvent()->getNumber()}. ročníku{/if}</h1>
    </div>

    <div id="content">
        {control pageForm}
        {$help}
    </div>
{/block}
